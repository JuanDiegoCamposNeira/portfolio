import type { ColorDefinition } from "./button.types";

export const ButtonColorDefinitions: ColorDefinition = {
  primary:
    "bg-transparent hover:bg-blue-300 hover:text-white active:bg-blue-100 py-2 px-4 rounded",
  secondary: "",
};
